import path from 'path';

export const assetsPath = path.join(__dirname, '../../assets/')

export const heartImage = path.join(assetsPath, 'heart.gif')
export const loadingImage = path.join(assetsPath, 'loading.gif')